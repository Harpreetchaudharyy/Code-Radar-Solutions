#include <stdio.h>

int main() {
    char alpha;
    scanf("%c", &alpha);
    if (char == 'a','e','i','o','u') {
        printf("Vowel");
    }
    else if ( char ) {
        printf("");
    }
    else {
        printf("Digit");
    }
    return 0;
}